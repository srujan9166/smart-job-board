package com.globalco.jobboard.entity;

/**
 * Defines the workspace engagement structure for jobs.
 */
public enum JobType {
    FULL_TIME,
    PART_TIME,
    CONTRACT,
    INTERNSHIP,
    REMOTE,
    TEMPORARY
}
